name: feature_guide
description: "Feature-discovery toolkit for Flutter — tips, banners, coach marks, and badges with session-aware gating."
version: 0.1.1

repository: https://github.com/TrackerJo/feature_guide

topics:
  - feature-discovery
  - onboarding
  - tooltips
  - coach-marks

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  overlay_support: ^2.1.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
