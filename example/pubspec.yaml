name: feature_guide_example
description: "Minimal demo of the feature_guide package."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  feature_guide:
    path: ../
  overlay_support: ^2.1.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
